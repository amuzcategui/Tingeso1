spring.application.name=lab1
server.port = 8090

spring.datasource.url=jdbc:postgresql://localhost:5432/lab1
spring.datasource.username=postgres
spring.datasource.password=ambar2004
spring.jpa.hibernate.ddl-auto=update



# ===== Keycloak realm issuer =====
# <issuer-uri> = http://<host>:<port>/realms/<realm>
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9090/realms/sisgr-realm

# Si tienes problemas de auto-descubrimiento, puedes usar jwk-set-uri expl�cito:
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:9090/realms/sisgr-realm/protocol/openid-connect/certs

# (Opcional) logs �tiles
logging.level.org.springframework.security=INFO
